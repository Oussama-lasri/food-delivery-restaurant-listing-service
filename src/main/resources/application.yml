server:
  port: 9091

spring:
  application:
    name: RESTAURANT-SERVICE
  profiles:
        active: dev
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
