server:
  port: 9091


spring:
  profiles:
    active: dev
  

spring:
  application:
    name: RESTAURANT-SERVICE
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
	database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
